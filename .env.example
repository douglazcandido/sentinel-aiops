# Lido pelo docker-compose.yml na raiz (nao pelo backend/.env, que so a aplicacao
# Python le). Caminho absoluto da PASTA data/ no host, usado pelo Airflow
# (DockerOperator) para montar o dataset nos containers efemeros das tasks.
SENTINEL_DATA_PATH=C:\Users\douglas\Documents\Scripts\Fiap\sentinel-aiops\backend\data
